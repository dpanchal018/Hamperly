# Development Guidelines

- Next.js 14+ App Router patterns
- TypeScript strict mode
- Server-side data fetching where possible

## Local Development
Docker is optional for Hamperly development. To run locally, configure .env.local with your Supabase Cloud credentials and run `npm run dev`.
